Task -3

using System;

namespace praktika2
{
    //parametrinde en ve hundurluk alib ,
    //asagidaki kimi bir piramida cap eden method yazin. meselen  en 5 , hundurluk 5 
    internal class Program
    {
        static void Main(string[] args)
        {
            for (int i = 1; i <= 5; ++i)
            {
                for (int j = 1; j <= i; ++j)
                {
                    Console.Write("9");
                }
                Console.WriteLine();
            }
        }
    }
}




using System;

namespace ConsoleApp2
{
    //Verilmiş massivdə elementlərin cəmini tapmaq üçün 
    internal class Program
    {
        static int accumulate(int[] arr)
        {
            int sum = 0;
            for (int i = 0; i < arr.Length; i++)
            {
                sum = sum + arr[i];
            }
            return sum;
        }

    
        public static void Main(String[] args)
        {
            int[] arr = { 12, 3, 4, 15 };
            int n = arr.Length;

            
            Console.Write("Toplami " + accumulate(arr));
        }
    }
}

